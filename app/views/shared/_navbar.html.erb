<nav class='navbar navbar-expand-lg navbar-light bg-light py-2'>
  <div class='container-fluid'>
    <%= link_to 'Botask', root_path, class: 'navbar-brand' %>
    <div class='collapse navbar-collapse' id='navbarNav'>
      <ul class='navbar-nav ml-auto pr-5'>
        <% if current_user.present? %>
          <li class='nav-item px-2'>
            <%= lineless_link_to(t('user.logout'), users_log_out_path, 'delete') %>
          </li>
        <% else %>
          <li class='nav-item px-2'>
            <%= lineless_link_to(t('user.login'), users_log_in_path) %>
          </li>
          <li class='nav-item px-2'>
            <%= lineless_link_to(t('user.registrations'), users_sign_up_path) %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
